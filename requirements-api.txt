# Dependencias SOLO para el contenedor de la API
# (sin ultralytics, whisper, opencv que son para el pipeline local)

# --- Base de datos ---
mysql-connector-python>=8.0
python-dotenv>=1.0

# --- API Backend ---
fastapi>=0.110
uvicorn>=0.27
gunicorn>=21.2
bcrypt>=4.1
python-jose[cryptography]>=3.3
python-multipart>=0.0.6
pyyaml>=6.0
